# 上传前验收状态（2026-09-05）

结论：源码更新达到上传条件，可以提交并推送 `main`。本次只上传源码，不创建版本标签或 GitHub Release；推送后仍以 GitHub Actions 的 Linux、容器和 PostgreSQL 结果作为跨环境复核。

## 已验证

- 待上传的 293 个文件已检查大小、敏感文件名、常见私钥/令牌格式及与本地配置密钥的精确匹配；未发现匹配。这不是完整安全审计，也不覆盖 Git 历史中的全部秘密类型。
- 不包含被忽略的本地数据集、模型、数据库、依赖目录或验收临时目录。
- 干净源码副本 `npm ci` 成功，npm 审计报告 0 个漏洞。
- 干净副本前端 42 项单元测试通过，生产构建成功；仍有第三方包体积警告。
- 知识库严格校验通过（得分 98）。
- 在隔离源码副本中，以 Mock 模型及防火墙进行核心依赖场景检查，`/` 与 `/api/health` 返回 200。
- 上一轮终端 14 项、浏览器 10 项通过，具体范围见 `PC_TERMINAL_ACCEPTANCE.md`。
- 本次重新运行终端行为和输入按键测试共 14 项通过，无跳过。离线安装脚本故障注入与 Bash 语法检查通过。

## 本轮修复进展

1. **可选 ML 依赖隔离已修复并验证。** 训练类改为按需导入，pandas 仅在读取/处理数据时加载。屏蔽 pandas/scikit-learn 等可选依赖后，`/api/ml/status`、`/api/ml/models`、`/api/ml/datasets` 均返回 200；状态明确为不可用、未加载。新增独立进程回归测试通过。
2. **开发测试依赖已补齐。** `requirements-dev.txt` 与 `pyproject.toml` 的 dev 附加依赖加入 pandas；数据集适配器不再要求完整训练栈才能导入。三个适配器的现有测试及目录注册检查通过。
3. 本轮终端行为与按键回归重新运行，14 项通过，无跳过；修复后的 wheel 构建成功。
4. **完整后端回归通过：753 passed，覆盖率 60.48%，退出码 0。** 使用 `pytest -m "not slow" --timeout=60 --cov=backend --cov-report=term --cov-fail-under=60`，没有降低覆盖率或安全阈值。测试运行于本机 Python 3.12，测试工具安装在隔离目录，基础依赖使用本机已有环境；不等同于干净 CI 环境验证。
5. 修复缺少额外信号时默认严重度稀释 LLM 置信度的问题；修正融合测试的加权结果预期，并补充阈值边界测试。测试之间隔离熔断器状态，明确测试中的当前日期/触发时间，保留封禁拒绝断言。

## 发布边界

- 新建 Python 虚拟环境的依赖下载安装受网络影响未完成；本机完整后端测试、覆盖率门禁、隔离 wheel 安装与 Mock CLI 检查已通过。
- Docker 引擎未运行，容器运行及 PostgreSQL 实测未通过验收。
- 未完成全新断网机器安装、真实模型服务验收与远程 GitHub CI。

SSH 私钥在当前执行环境中仍因 Windows 文件权限不可读，且 SSH Agent 未运行。上传时可尝试 Windows 已保存的 GitHub HTTPS 凭据；不会输出凭据、复制私钥、修改密钥权限或使用强制推送。
