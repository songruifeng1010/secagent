# 开发、测试与 CI 依赖（包含核心运行时）
-r requirements.txt

# 数据集适配器回归需要 pandas；不强制安装完整训练/向量模型栈。
pandas>=2.0.0

pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
pytest-timeout>=2.3.0
build>=1.2.0
bandit>=1.9.0
pip-audit>=2.10.0
cyclonedx-bom>=7.3.0
