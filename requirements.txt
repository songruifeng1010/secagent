# ============================================================
# SecAgentX 核心/生产依赖
# 开发与测试: pip install -r requirements-dev.txt
# 可选 ML 能力: pip install -r requirements-ml.txt
# ============================================================

# --- Web 框架 ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
websockets>=12.0
python-multipart>=0.0.9

# --- 数据库 ---
sqlalchemy>=2.0.0
aiosqlite>=0.20.0

# PostgreSQL 生产数据库
asyncpg>=0.29.0
# psycopg2-binary>=2.9.0  # 如需同步 SQLAlchemy 则取消注释

# --- 消息队列（告警接入） ---
# Kafka 消费者（如需要 kafka 告警接入则取消注释）:
# aiokafka>=0.10.0

# --- HTTP 客户端 ---
httpx>=0.27.0

# --- 配置 ---
python-dotenv>=1.0.0
pyyaml>=6.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# --- 通用 ---
rich>=13.7.0
prompt-toolkit>=3.0.43,<4
aiofiles>=23.2.0
numpy>=1.26.0

# --- Prometheus 监控 ---
prometheus-client>=0.20.0

# --- 跨区域联邦（按需安装） ---
# alibabacloud_ecs20140526>=3.0.0  # 阿里云防火墙适配器

# --- 数据库迁移 ---
alembic>=1.13.0
